<Window x:Class="PoliticsQuizApp.WPF.ImportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Import Câu Hỏi Từ Excel" Height="300" Width="500"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize">
    <Grid Margin="20">
        <StackPanel>
            <TextBlock Text="NHẬP KHẨU DỮ LIỆU TỪ EXCEL" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,20"/>

            <TextBlock Text="1. Chuẩn bị file Excel theo mẫu:" FontWeight="Bold"/>
            <TextBlock Text="Cột 1: Nội dung câu hỏi | Cột 2: ID Chủ đề | Cột 3: Độ khó (1-3)" Margin="10,5,0,0"/>
            <TextBlock Text="Cột 4-7: Đáp án A, B, C, D | Cột 8: Đáp án đúng (ghi A/B/C/D)" Margin="10,2,0,15"/>

            <TextBlock Text="2. Chọn file Excel (.xlsx):" FontWeight="Bold"/>
            <Grid Margin="0,5,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="txtFilePath" Height="30" Padding="5" IsReadOnly="True" Background="#F0F0F0"/>
                <Button Content="Chọn File..." Grid.Column="1" Width="80" Height="30" Margin="5,0,0,0" Click="BtnBrowse_Click"/>
            </Grid>

            <Button x:Name="btnImport" Content="BẮT ĐẦU IMPORT" Height="40" Background="#4CAF50" Foreground="White" 
                    FontWeight="Bold" IsEnabled="False" Click="BtnImport_Click"/>

            <TextBlock x:Name="lblStatus" Text="" HorizontalAlignment="Center" Margin="0,10" Foreground="Red"/>
        </StackPanel>
    </Grid>
</Window>